# Arduino build outputs
**/build/
*.bin
*.elf
*.map
*.merged.bin
*.bootloader.bin
*.partitions.bin

# Allow versioned firmware release binaries
!firmware/**/*.bin
!firmware/**/*.merged.bin

# VS Code
.vscode/
*.code-workspace

# OS files
.DS_Store
Thumbs.db

# Binary folders to exclude
*BIN FILE*
*latest wi compass working*
*latest*working*

# Old V2 backup folders
Source (montermanv2) - w logger-compass (good)/
Source(JanRush V2)/

# Claude Code local settings (machine-specific, never commit)
.claude/

# Claude AI workflow files — local only, not published to GitHub
CLAUDE.md
.claudeignore
docs/superpowers/
docs/CLI_compile_export.md

# foilIQ patron channel — private UUID, lives in foiliq private repo
Source/V2_Integration_Tx/BLE_Patron.ino

# Unknown artifact files — origin unclear, excluded until identified
ed SVG, update README tables, P5 plan doc added

# Local testing logs and field notes — data files, not source
Logs testing/
field tests RTM*.docx

# Local wiring reference — hardware build notes, not source
docs/Waterproof_Connector_Wiring.md
