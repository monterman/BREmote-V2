Made for BN-220 GPS and VESC

Logging only is possible when GPS has a fix, log files are saved as unix_time for filename

?start starts log
?stop stops log
?list shows log files
?download prints them as CSV on command line

with ?dbit options can be switched (start logging at boot, 1Hz or 0.1Hz log rate, dedug)